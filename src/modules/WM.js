import React from 'react';

const WM = () => {
    return ( <
        div >
        <
        h1 > WEB MINING < /h1> <
        p > Contenu du Module 1 ici. < /p> <
        /div>
    );
};

export default WM; // Correction ici